fastapi==0.115.5
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
pydantic==2.10.2
pydantic-settings==2.6.1
alembic==1.14.0
python-multipart==0.0.17
jinja2==3.1.6
playwright==1.61.0
python-escpos==3.1
pillow==12.3.0
# Windows has no built-in IANA time zone database, so zoneinfo.ZoneInfo(
# "Asia/Beirut") (see shared/timezone.py - every stored timestamp is UTC,
# converted to Beirut local time before it's shown to a human) would raise
# ZoneInfoNotFoundError without this. A no-op on platforms that already
# ship the database.
tzdata==2026.1
# Only needed for the "windows" printer connection type (talks to a
# thermal receipt printer already installed as a Windows printer) - the
# marker keeps `pip install` working on a non-Windows dev machine, which
# would otherwise fail here since this package doesn't exist off Windows.
pywin32==312; sys_platform == "win32"

# Dev / tests
pytest==8.3.4
httpx==0.27.2
